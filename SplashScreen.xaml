<Page
    x:Class="PAYETAXCalc.SplashScreen"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Border Padding="40">
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Spacing="24">

                <!-- App logo -->
                <Border Background="{ThemeResource AccentFillColorDefaultBrush}"
                        CornerRadius="16"
                        Width="180"
                        Height="180"
                        HorizontalAlignment="Center">
                    <Image Source="Assets/AppSplash.png"
                           Stretch="Uniform"
                           HorizontalAlignment="Center"
                           VerticalAlignment="Center"
                           Margin="24"/>
                </Border>

                <!-- App name and subtitle -->
                <StackPanel HorizontalAlignment="Center" Spacing="6">
                    <TextBlock Text="UK PAYE Tax Calculator"
                               FontSize="28"
                               FontWeight="Bold"
                               HorizontalAlignment="Center"/>
                    <TextBlock Text="Income Tax Estimation Tool"
                               FontSize="14"
                               Opacity="0.7"
                               HorizontalAlignment="Center"/>
                </StackPanel>

                <!-- Loading indicator -->
                <StackPanel HorizontalAlignment="Center" Spacing="8">
                    <ProgressRing IsActive="True" Width="40" Height="40" HorizontalAlignment="Center"/>
                    <TextBlock Text="Loading..."
                               FontSize="12"
                               Opacity="0.6"
                               HorizontalAlignment="Center"/>
                </StackPanel>

            </StackPanel>
        </Border>
    </Grid>
</Page>
